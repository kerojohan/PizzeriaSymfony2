{% extends 'PizzeriaTestBundle::layout.html.twig' %}
{% block titledins  %}Pizza details{% endblock %}
{% block dins -%}
    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>
    <div>
        <ul>{% for ingredient in entity.ingredients %}
            <li>{{ ingredient.name }}</li>
                {% endfor %}
            </ul>
            <p><b>Total price</b> {{ entity.sellingPrice }}€</p>
        </div>
        <ul class="record_actions">
            <li>
                <a href="{{ path('pizza') }}">
                    Back to the list
                </a>
            </li>
            <li>
                <a href="{{ path('pizza_edit', { 'id': entity.id }) }}">
                    Edit
                </a>
            </li>
            <li>{{ form(delete_form) }}</li>
        </ul>
        {% endblock %}
